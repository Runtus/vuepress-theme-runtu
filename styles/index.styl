@require './variables.styl'

* {
  padding: 0;
  margin: 0;
}

html, body {
    @media screen and (min-width: 600px) {
        min-width: 800px;
        overflow-x: hidden;
    }
}

.darkmode-layer, .darkmode-toggle {
  z-index: 1;
}

div {
  position: relative;
}

